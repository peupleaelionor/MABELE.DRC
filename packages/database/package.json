{
  "name": "@mabele/database",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:push": "prisma db push",
    "db:seed": "tsx seed.ts",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "prisma": "^5.14.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
